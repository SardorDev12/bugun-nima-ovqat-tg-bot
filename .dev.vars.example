# Local secrets for `wrangler dev`. Copy to `.dev.vars` (gitignored) — this
# is separate from `.env`, which only the Node scripts (migrate/seed/setup)
# read via dotenv.
BOT_TOKEN=
DATABASE_URL=
WEBHOOK_SECRET=
ADMIN_PASSWORD=
